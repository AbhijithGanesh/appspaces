name: Trigger auto deployment for container-app-edmhnkczvfj6w

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ fastapi ]
    paths:
    - '//**'
    - '.github/workflows/container-app-edmhnkczvfj6w-AutoDeployTrigger-986119f8-1dba-49ed-8c1a-2736bf9955d5.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPEDMHNKCZVFJ6W_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPEDMHNKCZVFJ6W_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPEDMHNKCZVFJ6W_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPEDMHNKCZVFJ6W_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPEDMHNKCZVFJ6W_REGISTRY_PASSWORD }}
          containerAppName: container-app-edmhnkczvfj6w
          resourceGroup: appspaces8dc5
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


